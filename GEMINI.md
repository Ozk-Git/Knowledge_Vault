# GEMINI.md

## 最初にすること
`AGENTS.md` を読み、内容を確認したことを報告してから作業を開始する。

---

## 実行メモ
ワーキングディレクトリは Vault のルートとして扱う。
詳細な規約は `AGENTS.md` および各サブディレクトリの `AGENTS.md` を正本とする。

---

## セッション開始（VS Code 向け軽量版）

VS Code 環境ではファイルアクセス回数に制限があるため、
`AGENTS.md` のセッション開始手順の代わりに以下の簡略版を実行する。

1. `index.md` を読み、Vault の概要を把握する
2. `log.md` の末尾5件を読み、前回の継続タスクを確認する
3. 以下を報告する:
   - Vault の現状サマリー（index.md より）
   - 前回からの継続タスク（log.md より）
   - 今セッションで着手できる作業の提案（1〜3件）

ディレクトリの列挙・ファイル横断検索（`#llm-draft` タグ検索・`Papers/*/03_gaps.md` など）は、
ユーザーから明示的に依頼があった場合にのみ実行する。
